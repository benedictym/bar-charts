/**
 * Composed set of all math utils, wrapped
 *
 * @module  mumath
 */
'use strict';
const wrap = require('mumath/wrap');

module.exports = {
	clamp: wrap(require('mumath/clamp')),
	within: wrap(require('mumath/within')),
	round: wrap(require('mumath/round')),
	precision: wrap(require('mumath/precision')),
	mod: wrap(require('mumath/mod')),
	log10: wrap(require('mumath/log10')),
	len: wrap(require('mumath/len')),
	closest: wrap(require('mumath/closest')),
	order: wrap(require('mumath/order')),
	lerp: wrap(require('mumath/lerp')),
	isMultiple: wrap(require('mumath/is-multiple')),
	normalize: wrap(require('mumath/normalize')),
	scale: wrap(require('mumath/scale')),
	// pretty: wrap(require('./pretty')),
};
